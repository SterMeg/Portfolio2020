// TODO: Add dark theme and switch
const ThemeSwitch = () => (
    {
      /* <label>
      Light
      <input
        type="radio"
        name="theme"
        value="light"
        checked={theme === "light"}
        onChange={e => setTheme(e.target.value)}
      />
    </label>
    <label>
      Dark
      <input
        type="radio"
        name="theme"
        value="dark"
        checked={theme === "dark"}
        onChange={e => setTheme(e.target.value)}
      />
    </label> */
    }
)

export default ThemeSwitch